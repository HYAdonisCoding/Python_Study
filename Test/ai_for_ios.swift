// 实现一个冒泡排序

let arrow = [1, 2, 3, 4, 5]
// 遍历数组arrow找出最大的数字
// 冒泡排序
for item in arrow.enumerated() {
    print("item \(item)")
    
}

// Swift实现一个埋点框架，支持上报埋点事件和额外参数。同时，发送网络请求模块可以替换。积累10条埋点记录后发送网络请求上报一次。还要支持持久化存储，当程序意外中断时可以存储在磁盘上，下次启动时还要从磁盘中读取数据
