version: 2

updates:
  - package-ecosystem: "pip"
    directory: "/other"
    schedule:
      interval: "weekly"

    ignore:
      - dependency-name: "*"
        update-types:
          - "version-update:semver-patch"
